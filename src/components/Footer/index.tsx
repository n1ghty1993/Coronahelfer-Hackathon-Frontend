import React, { FC } from 'react';
import './style.scss';

const Layout: FC = () => {
  return (
    <footer>
      <span>IMPRESSUM / DATENSCHUTZ</span>
    </footer>
  );
};

export default Layout;
