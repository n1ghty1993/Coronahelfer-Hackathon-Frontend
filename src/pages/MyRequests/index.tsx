import React, { useState, useEffect } from 'react';

const MyRequests = () => {
  const [requests, setRequests] = useState([]);

  useEffect(() => {
    const run = async () => {};

    run();
  }, []);

  return <span>Myrequests</span>;
};

export default MyRequests;
